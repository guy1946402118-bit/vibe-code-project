export const glass = {
  card: {
    background: 'rgba(13, 13, 13, 0.95)',
    backdropFilter: 'blur(12px)',
    WebkitBackdropFilter: 'blur(12px)',
    border: '1px solid var(--matrix-green-dim)',
    borderRadius: '8px',
    boxShadow: '0 4px 16px rgba(0, 0, 0, 0.5), inset 0 1px 0 rgba(255, 255, 255, 0.02)',
  },
  cardHover: {
    background: 'rgba(20, 20, 20, 0.98)',
    border: '1px solid var(--matrix-green-dim)',
    boxShadow: '0 0 15px var(--accent-dim), 0 4px 16px rgba(0, 0, 0, 0.5), inset 0 1px 0 rgba(255, 255, 255, 0.04)',
  },
  accent: {
    background: 'linear-gradient(135deg, var(--accent-dim), rgba(0, 255, 136, 0.03))',
    backdropFilter: 'blur(12px)',
    WebkitBackdropFilter: 'blur(12px)',
    border: '1px solid var(--matrix-green-dim)',
    borderRadius: '8px',
    boxShadow: '0 0 15px var(--accent-dim), 0 4px 16px rgba(0, 0, 0, 0.4)',
  },
  success: {
    background: 'linear-gradient(135deg, rgba(0, 255, 136, 0.06), var(--accent-dim))',
    backdropFilter: 'blur(12px)',
    WebkitBackdropFilter: 'blur(12px)',
    border: '1px solid rgba(0, 255, 136, 0.2)',
    borderRadius: '8px',
    boxShadow: '0 0 15px rgba(0, 255, 136, 0.06), 0 2px 8px rgba(0, 0, 0, 0.3)',
  },
  warning: {
    background: 'linear-gradient(135deg, rgba(255, 189, 46, 0.06), rgba(255, 170, 0, 0.02))',
    backdropFilter: 'blur(12px)',
    WebkitBackdropFilter: 'blur(12px)',
    border: '1px solid rgba(255, 189, 46, 0.2)',
    borderRadius: '8px',
    boxShadow: '0 0 15px rgba(255, 189, 46, 0.06), 0 2px 8px rgba(0, 0, 0, 0.3)',
  },
  danger: {
    background: 'linear-gradient(135deg, rgba(255, 51, 51, 0.06), rgba(255, 68, 102, 0.02))',
    backdropFilter: 'blur(12px)',
    WebkitBackdropFilter: 'blur(12px)',
    border: '1px solid rgba(255, 51, 51, 0.2)',
    borderRadius: '8px',
    boxShadow: '0 0 15px rgba(255, 51, 51, 0.06), 0 2px 8px rgba(0, 0, 0, 0.3)',
  },
  inner: {
    background: 'rgba(0, 0, 0, 0.3)',
    borderRadius: '6px',
    border: '1px solid var(--accent-dim)',
  },
  page: {
    padding: '24px',
    maxWidth: '1200px',
    margin: '0 auto',
  },
  title: {
    fontSize: '28px',
    fontWeight: 700 as const,
    color: '#e8e8e8',
    marginBottom: '8px',
    letterSpacing: '-0.02em',
  },
  subtitle: {
    color: '#666666',
    fontSize: '14px',
    fontFamily: "'Courier New', monospace",
  },
  btn: {
    background: 'transparent',
    backdropFilter: 'blur(8px)',
    WebkitBackdropFilter: 'blur(8px)',
    border: '1px solid var(--matrix-green-dim)',
    borderRadius: '4px',
    color: '#e8e8e8',
    padding: '10px 20px',
    fontSize: '14px',
    fontWeight: 500 as const,
    cursor: 'pointer',
    transition: 'all 0.15s ease',
    letterSpacing: '0.02em',
  },
  btnPrimary: {
    background: 'var(--accent-dim)',
    border: '1px solid var(--matrix-green)',
    borderRadius: '4px',
    color: 'var(--matrix-green)',
    padding: '10px 24px',
    fontSize: '14px',
    fontWeight: 600 as const,
    cursor: 'pointer',
    boxShadow: '0 0 15px var(--accent-dim)',
    transition: 'all 0.15s ease',
    letterSpacing: '0.03em',
  },
  btnAccent: {
    background: 'var(--accent-dim)',
    border: '1px solid var(--matrix-green-dim)',
    borderRadius: '4px',
    color: 'var(--matrix-green)',
    padding: '10px 20px',
    fontSize: '14px',
    cursor: 'pointer',
    transition: 'all 0.15s ease',
    fontFamily: "'Courier New', monospace",
  },
  input: {
    background: 'rgba(0, 0, 0, 0.5)',
    backdropFilter: 'blur(8px)',
    WebkitBackdropFilter: 'blur(8px)',
    border: '1px solid var(--accent-dim)',
    borderRadius: '4px',
    color: '#e8e8e8',
    padding: '12px 16px',
    fontSize: '14px',
    width: '100%',
    transition: 'all 0.15s ease',
  },
  gradientText: {
    background: 'linear-gradient(135deg, var(--matrix-green) 0%, #00ff88 100%)',
    WebkitBackgroundClip: 'text',
    WebkitTextFillColor: 'transparent',
  },
};
